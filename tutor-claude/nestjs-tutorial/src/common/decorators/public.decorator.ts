/**
 * ============================================================================
 * @Public() — 이 라우트는 인증 없이 접근 가능하다고 표시하는 커스텀 데코레이터
 * ----------------------------------------------------------------------------
 * 전역 JwtAuthGuard 를 걸어두면 "기본은 전부 인증 필요"가 됩니다.
 * 로그인/회원가입/헬스체크처럼 열려 있어야 하는 라우트에만 @Public() 을 답니다.
 *
 * SetMetadata 로 심은 값을 Guard 에서 Reflector 로 읽습니다.
 * ============================================================================
 */
import { SetMetadata } from '@nestjs/common';

export const IS_PUBLIC_KEY = 'isPublic';
export const Public = () => SetMetadata(IS_PUBLIC_KEY, true);
